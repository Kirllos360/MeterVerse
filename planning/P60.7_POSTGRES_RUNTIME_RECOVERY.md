# P60.7 — POSTGRES RUNTIME RECOVERY (reproducible procedure)

**Date:** 2026-08-15 · **Gate:** P60.7 · **Status:** BLOCKED_ENVIRONMENTAL (data safe)

## Exact diagnosis (re-verified this gate)
| Check | Result | Evidence |
|-------|--------|----------|
| Service `postgresql` (PG16) | **Stopped** | Get-Service |
| Service `postgresql-x64-18` (PG18) | Running on :5434 | TCP check (PID 6384) |
| PG16 :5433 | **DOWN** | TCP refused |
| PG16 data | INTACT: 6 base dirs, WAL present, no stale postmaster.pid | file scan |
| Disk | 52.9GB free (no disk issue) | Get-PSDrive |
| RAM | **1MB free / 13MB virtual / 21MB paging** | Win32_OS |
| Crash signature | 0xC0000142 (STATUS_DLL_INIT_FAILED) — child can't init DLLs | 11+ pg_ctl attempts, empty logs |
| DATABASE_URL | configured in backend/.env | file |

## Root cause
8GB RAM exhausted by OpenCode host (~1GB) + Edge (~1.5GB) + Defender (~0.2GB) + Memory Compression. PostgreSQL child process cannot allocate/init → 0xC0000142 + empty log. **Environmental, not code.**

## Reproducible recovery procedure
```
1. FREE RAM:
   - Close Edge browser tabs / extra apps
   - (optional) restart the machine to clear Memory Compression
   - Verify: Free RAM >= 300MB (Get-CimInstance Win32_OperatingSystem)

2. START PG16 (the :5433 MeterVerse DB — NOT postgresql-x64-18 which is PG18 :5434):
   Option A (elevated admin shell):
     net start postgresql
   Option B (pg_ctl, no admin needed once RAM is free):
     & "C:\Program Files\PostgreSQL\16\bin\pg_ctl.exe" start -D "C:\Program Files\PostgreSQL\16\data" -l "C:\Program Files\PostgreSQL\16\data\pg_startup.log" -w -t 60

3. VERIFY:
   psql -h localhost -p 5433 -U postgres -d meter_pulse -c "SELECT 1;"

4. VERIFY DATA SAFETY (P59-B frozen fingerprint):
   Customer 223 / Meter 277 / Invoice 116 / Payment 53 / Settlement 3 / ImportJob 2
   (Reading 362 = baseline 361 + 1 controlled P61 test reading)

5. VERIFY MIGRATION STATE:
   cd D:\meter\backend && npx prisma migrate status

6. THEN run the financial live-cert checklist:
   planning/P60.7_FINANCIAL_TRUTH_AND_CERTIFICATION.md §Live-cert
```

## Simulator/mock fallback — NOT used
A mock fallback would hide real runtime failures. Per §4, simulated verification is NEVER classified as production verification. The ingestion unit tests use mocked prisma (correct for logic); live E2E waits for PG.

## Backups (restore-ready evidence)
- meterverse_20261508.sql (today 09:49, 4.37MB): 223/277/361/116/53 + Settlement 3 + ImportJob 2, 325 tables, areaId/projectId present — full restore source.
- p59b_stage4b_pre_backfill_full_20260814_054557.sql (P59-B safety backup).
