# P60.7 — TENANCY SECURITY CERTIFICATION

**Date:** 2026-08-15 · **Gate:** P60.7 · **Method:** code trace + test evidence (P58 horizontal-privilege focus)

## Isolation mechanisms (verified)
| Mechanism | Purpose | Location | Verified |
|-----------|---------|----------|----------|
| `requireAccess(model, id)` | resource-level: denies if resource scope outside user scope; NULL/mismatch → DENY | security.js | code trace + security tests |
| `scopeWhere(req)` | list-level: injects user's area/project scope into all queries | security.js | code trace |
| `clampRequestedScope(req)` | query-level: clamps client-supplied ?areaId/?projectId to user scope | security.js | code trace |
| `requirePermission` | RBAC: hardcoded ROLE_PERMISSIONS + DB custom grants | security.js | 49/49 security tests |
| P58 ingestion safety | payload cannot override meter-owned tenancy | symbiot-bridge + test | P58 test (P60.6) |

## Attack attempts (classification)
| Attempt | Protection | Status |
|---------|-----------|--------|
| ID substitution (customer/meter/invoice) | requireAccess fail-closed | PROTECTED (code) |
| Serial substitution (ingestion) | findUnique(serial) → unknown rejected | PROTECTED (test) |
| Query param areaId/projectId substitution | clampRequestedScope | PROTECTED (code) |
| Body tenant override (ingestion) | meter-owned areaId wins | PROTECTED (P58 test) |
| Route param substitution | requireAccess on :id resources | PROTECTED (code) |
| Bulk/export endpoint abuse | requirePermission (list/export) + scopeWhere | PROTECTED (code) |
| Report endpoint abuse | requirePermission + scope | PROTECTED (code) |

## Status
- **CODE-LEVEL: CERTIFIED** — all tenancy/security mechanisms present + enforced + tested (49/49 security+tenancy tests, P58 ingestion test).
- **RUNTIME-LIVE: NOT PROVEN** (PG down blocks authenticated cross-tenant live tests). Classified honestly.
- The P58 horizontal-privilege issue is specifically closed for ingestion (payload override blocked); the general requireAccess fail-closed invariant is the P59-B foundation.

## Recommendation
- Add explicit **cross-tenant negative runtime tests** when PG returns (viewer of area-a accessing area-b invoice → 403). Prepared in financial live-cert checklist.
