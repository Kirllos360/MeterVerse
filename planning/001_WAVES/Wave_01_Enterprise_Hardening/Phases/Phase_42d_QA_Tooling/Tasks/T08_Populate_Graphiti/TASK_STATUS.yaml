task:
  name: "Populate Graphiti Knowledge Graph"
  phase: "42d"
  status: "PLANNING"
  priority: "MEDIUM"
  risk: "Architecture graph is empty — no traceability between components"
  steps:
    S01: "Map all 78 Prisma models as graph nodes — COMPLETE when all models in graph"
    S02: "Map all 16 route files as API nodes — COMPLETE when all routes linked"
    S03: "Map all 53 admin pages as UI nodes — COMPLETE when all pages linked"
    S04: "Generate graph visualization — COMPLETE when HTML viewer renders"
  completion_criteria:
    - "Graphiti index.json contains node entries for all models, routes, pages"
    - "Edges connect models -> routes -> pages"
    - "HTML visualization renders in browser"
