# P60.1 — GRAPH / SPECKIT DELTA AUDIT

**Date:** 2026-08-15 · **Gate:** P60.1 · **Method:** trace actual code against graph nodes/edges (not just validator)

## Graph Audit (§13) — Discrepancy Table

| Graph expects | Actual repository | Difference | Impact | Action |
|---------------|-------------------|------------|--------|--------|
| Tenancy: Area→Customer, Area→Meter (canonical) | Customer/Meter have areaId; requireAccess fail-closed | none | none | CONFIRMED |
| Tenancy: Project subordinate, non-authorizing | projectId never expands scope; NULL → DENY | none | none | CONFIRMED |
| RBAC: User→Role→Permission→JWT→requireAccess/scopeWhere/clamp | requireRole/requirePermission/requireAccess/scopeWhere/clampRequestedScope all in security.js | none | none | CONFIRMED |
| ImplDep: Settlement/ChargeRule/TestIsolation/Solar-wallet DONE | services exist + tests | none | none | CONFIRMED |
| ImplDep: P59-B approvals #2-#6 BLOCKED | decisions PENDING (STATE 2) | none | none | CONFIRMED |
| ImplDep: OBIS register BLOCKED | OBIS = STATE 2 (never fabricated) | none | none | CONFIRMED |
| ImplDep: Solar Excel ImportJob [safe] | import-engine.js solar_* | none | none | CONFIRMED |
| LEGACY-REUSE graph | Collection reuse matrix now has 1 NEW item (templates) | graph lacks template-gen node | LOW | **GRAPH DEFECT: LEGACY-REUSE should note template download reuse** |

**GRAPH DEFECT (1):** `LEGACY-REUSE.dot` does not include the Collection template-download → MeterVerse `generateTemplate` reuse edge added in P60.1. Evidence: reuse matrix row "downloadable fillable xlsx templates" = REUSE BUSINESS LOGIC (NEW). Corrected model: add a node `TemplateGen` with edge `Collection templates → MeterVerse import-engine.generateTemplate`.

## SpecKit Audit (§14)

| Spec | Graph | Implementation | Test | Runtime | Status |
|------|-------|----------------|------|---------|--------|
| Collection convergence (P61) | matched | add-data REAL, upload REAL | 371 backend, browser | live (DB up earlier) | IMPLEMENTED |
| Template download (P60.1) | needs edge | generateTemplate + route + FE button | 3 unit tests | **200/16926B live** | IMPLEMENTED |
| SpecKit validator | — | — | — | 100% | PASS |

## Delayed SpecKit update
- SpecKit MASTER-ROADMAP + index should gain a "Template download/generation" entry matching the graph edge + this gate's implementation. (Documented; safe to add in governance commit.)

## Conclusion
Graph and SpecKit and repository **agree** on all core invariants (tenancy fail-closed, RBAC backend-enforced, implementation dependency order). One graph defect (missing template-gen reuse edge) and one SpecKit gap (template entry) — both documented, both addressed in the governance/artifact commit.
