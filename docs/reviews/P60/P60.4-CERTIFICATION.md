# P60.4 — COLLECTION SETTLEMENT + PAYMENT + CHEQUE CLOSURE

**Date:** 2026-08-15 · **Gate:** P60.4 · **HEAD:** 0a9b5f25 → (P60.4 commits)
**Mode:** implementation-first (anti-stall), Collection = behavior source (no clone)

## 1. Starting State (verified)
- HEAD `0a9b5f25`, clean. PG :5433 BLOCKED/env (recorded once, no repeat attempts).
- Settlement engine (calculate/apply/materialize) + route (CRUD + apply/:invoiceId) — DONE (LR-2).
- Payment route (create/reverse/refund/statement/aging/list/export) — DONE (multi-invoice oldest-due-first allocation via PaymentTransaction, overpayment→ledger credit, GL posting).
- Cheque ENGINE (create/clear/reject) — DONE (LR-7, 6 tests) but **NO ROUTE** (engine not exposed via HTTP).

## 2. Forensic Baseline (Collection = evidence)
- Collection PaymentAllocation (payment_id, invoice_id, allocated_amount, allocation_order) — MeterVerse implements the SAME behavior via PaymentTransaction (oldest-due-first by dueDate asc). Class: **behavior already present, architecture different (cleaner)**.
- Collection Cheque (payment_id, cheque_number, bank_name, status, cleared_date) — MeterVerse maps to Payment(method=cheque, reference=cheque#, notes=bank). Class: **behavior present; HTTP surface was MISSING**.

## 3. What Was Implemented (P60.4)
**CHEQUE ROUTE (`backend/src/routes/cheque.js`)** — the missing HTTP surface:
- `GET /api/cheques` — list (customerId/status filter, tenancy-clamped, paginated)
- `POST /api/cheques` — create cheque payment (zod: customerId, amount, chequeNumber required; bankName, invoiceId, notes optional) → engine
- `POST /api/cheques/:id/clear` — clear (status→completed, paidAt) — idempotent
- `POST /api/cheques/:id/reject` — reject/bounce with reason
- Auth: `authenticate` + `requirePermission("payments.*")` + `requireAccess`; audit on every mutation.
- Mounted in server.js at `/cheques`.

**TESTS (`backend/tests/api/cheque-route.test.mjs`)** — 8 tests: create, missing-chequeNumber 400, unknown-customer 404, clear, non-cheque clear 400, reject, list, unauthenticated 401.

## 4. Test Results
- Full suite: **361 pass / 18 skip (379)** — up from 353/371 (+8 cheque-route)
- cheque-engine 6 + cheque-route 8 = **14 cheque tests**
- Live: `/api/cheques` mounted (401=auth, not 404)
- Graph 12/0/0 · SpecKit 100%

## 5. Graph/SpecKit Updates
- SpecKit C-CHEQUE: PENDING(evidence) → **ENGINE+ROUTE DONE (P60.4)**, POS/Currency still evidence-gated
- IMPLEMENTATION-DEPENDENCY: P3_CHEQUE → DONE (green); added P3_POS (evidence-gated)
- TEST-COVERAGE: GAP3 split — cheque now T_CHEQUE→CHEQUE_DONE; POS/chilled remain gap

## 6. Financial Vertical Status (post-P60.4)
Invoice → Outstanding → Payment → Allocation (oldest-first) → Settlement → Ledger → Receipt/statement
| Capability | Status |
|-----------|--------|
| Payment create + allocation | DONE (live, DB-verified P61) |
| Payment reverse/refund | DONE |
| Statement + aging | DONE |
| Settlement engine + route | DONE |
| Cheque engine | DONE (LR-7) |
| **Cheque HTTP route** | **DONE (P60.4) — NEW** |
| Collection/cheque UI | PARTIAL (FE pages exist; runtime DB-gated) |

## 7. Remaining (classified)
- **BLOCKED (env):** PostgreSQL :5433 (data safe, backup-proven)
- **BLOCKED (approval):** Import EXECUTE, OBIS, P59-B #2-6, Wave 4
- **EVIDENCE-GATED:** POS/CurrencyType, chilled-water, water-balance
- **KNOWN DEBT:** cross-root type:module, apiClient double-prefix hygiene
- **NOT-DONE:** cheque UI wiring (DB-gated live verify), solar e2e

## 8. Delta (BEFORE → AFTER)
- Capabilities: cheque HTTP surface 0 → **4 endpoints**
- APIs implemented: +4 (cheque list/create/clear/reject)
- Tests added: +8 (cheque-route); cheque total 6→**14**
- Full suite: 371 → **379**
- Collection capabilities converged: cheque (was engine-only) → **engine+route+tested**
