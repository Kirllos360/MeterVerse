# P60.6 — FINANCIAL LIVE-CERTIFICATION CHECKLIST
# Runs ONLY when PostgreSQL :5433 is available. Prepared during the DB-blocked gate.
# Usage: start PG -> login -> run each step; every step must return a real result.

## Preconditions
- [ ] PostgreSQL :5433 UP (`net start postgresql` elevated) — PG16, NOT postgresql-x64-18
- [ ] Fingerprint intact: Customer 223 / Meter 277 / Invoice 116 / Payment 53 / Settlement 3 / ImportJob 2
- [ ] Admin BE :3131 + FE :3535 UP
- [ ] `npx vitest run tests/unit tests/api` = 398 (380 pass / 18 skip)

## 1. Authentication
- [ ] POST /api/auth/login (admin@meterverse.com / Admin@123) → 200 + accessToken
- [ ] Negative: bad password → 401; no token on /api/customers → 401

## 2. Financial vertical (live)
- [ ] GET /api/customers?limit=5 → real customers
- [ ] GET /api/invoices?limit=5 → invoices with amounts + status
- [ ] GET /api/payments/customers/:id/statement → balance + aging buckets
- [ ] POST /api/payments {customerId, amount, method:"cash"} → payment created; verify:
  - oldest-due-first allocation (PaymentTransaction rows)
  - invoice paidAmount incremented, status → paid/partial
  - overpayment → CustomerLedgerEntry credit
- [ ] POST /api/cheques {customerId, amount, chequeNumber, bankName} → 201 pending
- [ ] POST /api/cheques/:id/clear → status completed, paidAt set
- [ ] POST /api/cheques/:id/reject {reason} → status rejected
- [ ] POST /api/cheques/:id/clear on non-cheque → 400
- [ ] GET /api/cheques → list with filter by status
- [ ] POST /api/settlements + POST /api/settlements/:id/apply/:invoiceId → invoice total recomputed + InvoiceSettlement rows

## 3. Security / tenancy (live)
- [ ] Unauthorized: GET /api/payments without token → 401
- [ ] GET /api/cheques without token → 401
- [ ] Wrong area scope (viewer role) → 403 / empty scoped set (fail-closed)
- [ ] Cross-customer statement access → denied for non-scoped role

## 4. Audit
- [ ] payment.created / cheque.created / cheque.cleared / cheque.rejected audit rows exist

## 5. Result
Every row checked = financial vertical LIVE-CERTIFIED. Update P60.6 artifact accordingly.
