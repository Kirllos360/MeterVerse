{
  "name": "@meterverse/portal-backend",
  "version": "10.0.0",
  "private": true,
  "description": "MeterVerse OS — Portal API (port 3003). Deployable profile over the shared Express source (PORTAL_MODE=1); no code duplication.",
  "scripts": {
    "dev": "cd ../../backend && PORT=3003 PORTAL_MODE=1 node src/server.js",
    "start": "cd ../../backend && PORT=3003 PORTAL_MODE=1 node src/server.js",
    "test": "cd ../../backend && npm test"
  },
  "dependencies": {
    "@meterverse/auth": "10.0.0",
    "@meterverse/runtime": "10.0.0",
    "@meterverse/shared-types": "10.0.0"
  }
}
